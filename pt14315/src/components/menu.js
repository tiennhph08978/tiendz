import React from 'react'
import PropTypes from 'prop-types'

const menu = props => {
    return (
        <div>
            <h1>Menu</h1>
        </div>
    )
}

menu.propTypes = {

}

export default menu
